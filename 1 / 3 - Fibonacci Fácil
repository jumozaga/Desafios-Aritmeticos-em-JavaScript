let n = parseInt(gets());
let i = 0
let fibo=[]

while (i < n){
  
    if (i == 0 || i == 1){
        fibo.push(i)
    }
    
    if (i > 1){
        let aux = fibo[i-2] +fibo[i-1]
        fibo.push(aux)
    }
    i++
  
}

fibo = fibo.join(" ");
console.log(fibo)